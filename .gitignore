# Dependencies & build output
node_modules/
.next/
dist/
out/

# Env — secrets never get committed
.env
.env.local
.env.*.local

# Logs & OS junk
*.log
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Claude Code per-user settings (CLAUDE.md and .claude/CLAUDE.md are tracked)
.claude/settings.local.json

# Beads dolt-managed data files (issues.jsonl is the source of truth)
.beads/*.db
.beads/*.db-journal

# Local tmp / scratch
.tmp_*
*.tmp

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
